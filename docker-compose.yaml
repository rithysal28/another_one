version: '3.8'

services:
  web:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "8092:8092"
    environment:
      - DATABASE_URL=${DATABASE_URL}
    command: sh -c "npm install && npm start"
    restart: always